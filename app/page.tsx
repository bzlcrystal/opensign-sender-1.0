"use client";import{useState}from"react";export default function Home(){const[busy,setBusy]=useState(false),[msg,setMsg]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const r=await fetch("/api/send-document",{method:"POST",body:new FormData(e.currentTarget)});const x=await r.json();setMsg(x.url?x.message+"\n"+x.url:x.error||x.message);setBusy(false)}return <main className="card"><h1>Envoyer pour signature</h1><p>Le PDF doit déjà être rempli.</p><form onSubmit={go}><div className="row"><label>Modèle<select name="model"><option value="devis">Devis</option><option value="resa">Réservation</option></select></label><label>PDF<input name="file" type="file" accept="application/pdf" required/></label></div><div className="row"><label>Nom<input name="signerName" required/></label><label>Email<input name="signerEmail" type="email" required/></label></div><label>Objet<input name="title"/></label><button disabled={busy}>{busy?"Envoi…":"Envoyer"}</button></form>{msg&&<pre className={msg.startsWith("Erreur")?"msg err":"msg"}>{msg}</pre>}</main>}